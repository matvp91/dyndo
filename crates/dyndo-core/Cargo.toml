[package]
name = "dyndo-core"
version = "0.1.0"
edition = "2021"

[dependencies]
mp4-atom = "0.12.1"
serde = { version = "1", features = ["derive"] }
thiserror = "2"
tokio = { version = "1", features = ["fs", "io-util", "rt", "macros"] }

[dev-dependencies]
serde_json = "1"
tempfile = "3"
